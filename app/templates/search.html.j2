<!DOCTYPE html>
<html lang="">
  <head>
    <meta charset="utf-8">
    <title>Similar Anime</title>
    <style type="text/css">body{margin:40px
auto;max-width:1000px;line-height:1.6;font-size:18px;color:#444;padding:0
10px}h1,h2,h3{line-height:1.2}</style>
    <style>
      .container {
          display: flex;
          flex-wrap: wrap;
          justify-content: space-between;
      }
      .card {
          width: calc(15% - 20px);
          margin: 10px;
          text-align: center;
          border: 1px solid #ccc; /* Thin border around each card */
          padding: 10px; /* Optional: Adds some space inside the border */
      }
      .card img {
          width: 100%;
          height: auto;
          cursor: pointer;
      }
      .card a {
          display: block;
          margin-top: 10px;
          text-decoration: none;
          color: #007BFF; /* Blue color for the link */
      }
      .card .show-name {
          overflow: hidden;
          text-overflow: ellipsis;
          white-space: nowrap;
          width: 100%;
          font-size: 14px;
      }
  </style>
  </head>
  <body>
    <header></header>
    <main>
      <h3> 
        Search similar anime 
      </h3>
      <ul>
        <li>Search for an anime by its name</li>
        <li>Click on a search result to find similar anime</li>
      </ul>
      <form action="/" method="POST">
        <input name="search" type="search">
        <input type="submit" value="Search">
      </form>

      <div class="container">
      {% for result in results %}
        <div class="card">
            <a href="{{ url_for('get_similar', anime_id=result[0]) }}">
                <img src={{ result[3] }} alt={{ result[1] }}>
            </a>
            <div class="show-name">{{ result[1] }}</div>
            <a href={{ result[2] }} target="_blank">Open on MAL</a>
        </div>
        <!-- Repeat the card structure for each anime show -->

      {% endfor %}
      </div>
      
    </main>
    <footer></footer>
  </body>
</html>
